# crm-cleanup (runx skill)

CRM cleanup that reads current records from a real source at run time, reconciles a call transcript deterministically, and executes the decided updates through a sealed mock-CRM transport with before/after evidence.

Package: `skills/crm-cleanup/`. Published to the hosted runx registry as `antheducation/crm-cleanup`.
